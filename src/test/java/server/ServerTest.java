package server;

import org.junit.Test;
import java.io.IOException;

public class ServerTest {

    @Test
    public void connectionsFromOtherContainer() throws IOException {
        //Server server = new Server("database", 3000);
        //server.start();
        //while (server.isAlive());
    }
}